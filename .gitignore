# --- 1. Global Dependencies (Chặn node_modules ở mọi nơi) ---
**/node_modules
npm-debug.log*
yarn-error.log*
yarn-debug.log*
*.log

# --- 2. Database Data (QUAN TRỌNG - Rất nặng) ---
# Chặn folder dữ liệu Postgres ở root
/postgres_data/
/data_fresh_v2/
/redis_data/

# --- 3. Environment Variables (BẢO MẬT) ---
# Chặn tất cả file .env ở bất kỳ thư mục nào (root, server, client)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
*.env

# --- 4. System & IDE Files ---
.DS_Store
Thumbs.db
.vscode/
.idea/

# --- 5. Build Outputs (Nếu lỡ build ở root) ---
dist/
build/
coverage/

# --- 6. Các file rác/nháp cụ thể trong dự án của bạn ---
# Các file txt ghi chú (local notes - không cần thiết trên remote)
note.txt
stripe.txt
typeOrm.txt
kienthuc.txt
noteErr.txt
redis.txt
ggLogin.txt
migration.txt
middlewareNextJs.txt
cdn.txt
flowCartRedis
backupCart-Checkout.txt

# File thực thi (như stripe.exe)
*.exe
stripe.exe

# Thư mục kế hoạch local
changePlan/

# File tạm
*.tmp